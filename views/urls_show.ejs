<!DOCTYPE html>
<html lang="en">
<head>
<%- include('./partials/_header', { username: username }) %>
</head>
<body style="font-size:20px; padding:50px;">

   <!-- Add EJS code here -->
   <a href="/urls/new">Create new TinyURL</a>
   <p>ShortURL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>

    <form class="form-inline" action="/urls/<%= shortURL %>" method="POST">
      <div class="form-group mb-2">
        <label for="longURL">Enter a URL:</label>
        <input value="<%= longURL %>" class="form-control" type="text" name="longURL" placeholder="http://" style="width: 300px; margin: 1em">
        <button type="submit" class="btn btn-primary">Edit</button>
      </div>
    </form>

</body>
</html>